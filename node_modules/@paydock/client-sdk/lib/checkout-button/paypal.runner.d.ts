import { IRunner } from "./runner";
import { PopupRunner } from "./popup.runner";
export declare class PaypalRunner extends PopupRunner implements IRunner {
    getSuccessRedirectUri(): string;
    getErrorRedirectUri(): string;
}
